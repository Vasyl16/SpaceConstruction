import { footerData } from './footer/footerContextData';
import { navItems } from './header/header';

export const singleNewsContext = {
  headerData: {
    navItems,
    currentPath: 'news/single-news',
  },

  footerData,
};
